name: 🚀 Veille Metz - Auto Scraper

# Déclenche l'exécution automatiquement
on:
  # Planification : tous les lundis et jeudis à 08h UTC (10h CET)
  schedule:
    - cron: '0 8 * * 1,4'
  
  # Permet exécution manuelle depuis GitHub
  workflow_dispatch:

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    
    steps:
    # 1. Télécharger le code du repo
    - name: ✅ Checkout repo
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    # 2. Setup Python
    - name: 🐍 Setup Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    # 3. Installer les dépendances
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 lxml
    
    # 4. Lancer le scraper
    - name: 🔍 Run scraper
      run: python scraper.py
    
    # 5. Commit et push les changements
    - name: 💾 Commit and push changes
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add data.json
        git commit -m "🔄 Auto-update veille $(date +'%d/%m/%Y %H:%M')" || echo "No changes to commit"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
